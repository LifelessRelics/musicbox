pub trait Mode {
    fn _ionian(&mut self);
    fn _dorian(&mut self);
    fn _phyrgian(&mut self);
    fn _lydian(&mut self);
    fn _mixolydian(&mut self);
    fn _aeolian(&mut self);
    fn _locrian(&mut self);
}
